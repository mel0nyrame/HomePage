server:
  port: 8801

# 初始化管理员账号密码
app:
  admin:
    init-account: admin
    init-password: password

spring:
  application:
    name: homepage-user
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/homepage?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root # 测试环境用户名
    password: 123 # 测试环境密码
  web:
    resources:
      add-mappings: false